import 'package:flutter/material.dart';

class Impostazioni extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Queste sono le settings!!"),
      )
    );
  }
}
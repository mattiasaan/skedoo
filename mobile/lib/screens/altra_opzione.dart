import 'package:flutter/material.dart';

class AltraOpzione extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Questa è un altra pagina!!"),
      )
    );
  }
}